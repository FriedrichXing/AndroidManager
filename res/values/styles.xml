<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

   <!--  <style name="telNumStyle">
        <item name="android:layout_width">@string/telNum_layout_width</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@string/telNum_layout_textSize</item>
        <item name="android:textColor">@string/telNum_layout_textColor</item>
        <item name="android:background">@drawable/selector_bg</item>
        <item name="android:layout_weight">@string/telNum_layout_weight</item>
        <item name="android:padding">@string/telNum_layout_padding</item>
        
    </style>
    --> 
    <style name="telNumStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#4A4E50</item>
        <item name="android:background">@drawable/selector_bg</item>
        <item name="android:layout_weight">1</item>
        <item name="android:padding">10dp</item>
        
    </style>

</resources>